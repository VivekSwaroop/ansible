- name: loops
  hosts: localhost
  connection: local
  tasks:
  - name: print persons
    ansible.builtin.debug:
      msg: "Hello {{item}}" # item is a default keyword
    loop:
    - Ramesh
    - Suresh
    - Robert
    - Raheem