# playbook is a list of plays
- name: play 1
  hosts: localhost
  tasks:
    name: play 1 and task 1
    ansible.builtin.debug:
      msg: "Hello from PLAY-1 and TASK-1"
  
- name: play 2
  hosts: localhost
  tasks:
    name: play 2 and task 
    ansible.builtin.debug:
      msg: "Hello from PLAY-2 and TASK-2"

    